import { View } from "react-native";
import Header from "../../components/header/Header";

const JournalScreen = () => {
  return (
    <View style={{ flex: 1, backgroundColor: "transparent" }}>
      <Header title="Günlük" />
      {/* Add other content for the journal screen here */}
    </View>
  );
};

export default JournalScreen;
